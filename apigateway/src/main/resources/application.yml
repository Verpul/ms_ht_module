server:
  port: 8090

spring:
  cloud:
    gateway:
      globalcors:
        corsconfigurations:
          '[/**]':
            allowedOrigins: "*"
            allowedHeaders: "*"
            allowedMethods: "*"
      routes:
        - id: weight
          uri: http://localhost:8100
          predicates:
            - Path=/weight
